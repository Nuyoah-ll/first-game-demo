import { _decorator, Component, Node, tween } from 'cc';
import { UIBase } from '../common/classes/ui/UIBase';
const { ccclass, property } = _decorator;

@ccclass('SettingPage')
export class SettingPage extends UIBase {
    protected onLoad(): void {
        super.onLoad();
    }
}
